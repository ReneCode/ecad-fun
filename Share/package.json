{
  "name": "multiplayer",
  "version": "0.0.24",
  "description": "multiplayer for ecad.fun",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "_prepare": "npm run copy",
    "build": "tsc",
    "build:prod": "CI=true && tsc",
    "copy": "./copy-files.sh",
    "test": "jest --config=jest.config.json --verbose",
    "test:w": "jest --verbose --watchAll --config=jest.config.json"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  }
}
