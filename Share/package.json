{
  "name": "multiplayer",
  "version": "0.0.24",
  "description": "multiplayer for ecad.fun",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "prepare": "npm run copy",
    "build": "tsc",
    "copy": "./copy-files.sh",
    "test": "jest --config=jest.config.json --verbose",
    "test:w": "jest --verbose --watchAll --config=jest.config.json"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.0",
    "typescript": "^3.9.5"
  }
}
